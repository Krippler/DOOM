<?xml version="1.0"?>
<Container version="2">
  <Name>DOOM</Name>
  <Repository>ghcr.io/krippler/doom:latest</Repository>
  <Registry>https://github.com/Krippler/DOOM/pkgs/container/doom</Registry>
  <GitHub>https://github.com/Krippler/DOOM</GitHub>
  <Project>https://github.com/Krippler/DOOM</Project>
  <Support>https://github.com/Krippler/DOOM/issues</Support>
  <ReadmeURL>https://github.com/Krippler/DOOM/blob/master/DOCKER.md</ReadmeURL>
  <Beta>False</Beta>
  <Network>bridge</Network>
  <MyIP/>
  <Shell>sh</Shell>
  <Privileged>false</Privileged>
  <Overview>
    The original 1997 linuxdoom-1.10 sources, built from source and playable in a browser.&#13;
    &#13;
    The engine only ever supported an 8-bit PseudoColor X visual, which no current X server offers, so the container runs its own Xvfb at depth 8 and exports it over noVNC. Nothing needs to be installed on the host.&#13;
    &#13;
    Sound comes out of the browser, with nothing to set up. Effects and music - the music converted from the WAD's MUS lumps to MIDI and rendered by FluidSynth - are mixed inside the container and sent to the same page as the picture, over the WebUI port you already mapped. Click to start playing and the sound starts with it, because browsers will not play audio without a click.&#13;
    &#13;
    The shareware game data is included, so this plays as soon as it starts - episode 1, nine levels. To play the full game, put your own DOOM.WAD, DOOM2.WAD, TNT.WAD or PLUTONIA.WAD into the WADs folder below and it will be used instead. The name is matched without regard to case. Commercial game data is not included and must come from your own copy of the game.&#13;
    &#13;
    Click into the canvas before typing: noVNC only forwards the keyboard once it has focus. Ctrl fires and Alt strafes, which browsers like to intercept, so use the modifier pad in the noVNC toolbar if a key will not reach the game.&#13;
    &#13;
    Keys and the mouse are set from inside the game, under Options - Setup. The same menu lists the WAD files in the folder below and loads the one you pick, so mods can be dropped in and switched to without touching the container settings.&#13;
    &#13;
    The WebUI opens a client that captures the mouse, so turning does not run out of screen and the cursor stays in the game. Esc hands the cursor back, and the ` key opens the game menu whatever the mouse is doing. Plain noVNC is still there at /vnc.html if you would rather it did not grab the pointer, though it carries no sound.
  </Overview>
  <Category>GameServers: Other:</Category>
  <WebUI>http://[IP]:[PORT:6080]/</WebUI>
  <TemplateURL>https://raw.githubusercontent.com/Krippler/DOOM/master/templates/unraid.xml</TemplateURL>
  <Icon>https://raw.githubusercontent.com/Krippler/DOOM/master/templates/doom-icon.png</Icon>
  <ExtraParams>--restart=unless-stopped</ExtraParams>
  <PostArgs/>
  <CPUset/>
  <DateInstalled/>
  <DonateText/>
  <DonateLink/>
  <Requires>Nothing. The shareware game data ships with the container. Add your own IWAD to the WADs folder to play the full game.</Requires>
  <Changes>
### 1.10.15
- Sound comes out of the browser. Effects and music are mixed in the container
  and sent to the same page as the picture, over the WebUI port you already
  mapped - nothing to install, nothing to mount. It starts with the click that
  starts play.

### 1.10.14
- The ` key opens the game menu, in every mode. Escape still does when the
  browser lets it through, which in a window it does not.
- Every menu is drawn at one size, and the added options fit.

### 1.10.13
- You can now play in the window instead of fullscreen; both capture the mouse.

### 1.10.12
- Fixes a crash loop caused by the Graphic Detail option in the game's own
  Options menu. An affected config repairs itself; nothing to edit.

### 1.10.11
- A rendering glitch no longer ends the game, and the log says what caused it.

### 1.10.10
- A bad screen-size value in the config no longer breaks the renderer or puts
  the container into a crash loop.

### 1.10.9
- The mouse turns you without also walking you forward and back. Options -
  Setup - Mouse - MOVE WITH MOUSE restores the original behaviour.
- PUID=0 starts instead of looping.

### 1.10.8
- Holding a mouse button repeats the action, and releasing it stops.

### 1.10.7
- Mouse look actually works. Most of the movement was being discarded before
  it reached the game.
- The picture fills the window instead of sitting small in the corner.
- The mouse capture no longer fails silently when you click to play.

### 1.10.6
- Mouse look turns properly. Most of the movement was being discarded before
  it reached the game.
- Mouse buttons no longer stick on after the mouse capture is released.
- Fullscreen fills the screen, keeping the picture's shape.

### 1.10.5
- Mouse look turns properly instead of jittering in place.

### 1.10.4
- Escape opens the game menu again while the mouse is captured, instead of
  letting the mouse go. Hold Escape to release it and leave fullscreen.
- The menu can be put on a second key of your choosing, under
  Options - Setup - Controls, for browsers that will not give Escape up.

### 1.10.3
- The Ultimate Doom is recognised properly. Re-releases install it as DOOM.WAD,
  which the engine read as three-episode Doom, so Thy Flesh Consumed could not
  be reached. It is identified by its contents now.
- With both games in the WADs folder, Doom starts rather than Doom II. Set
  DOOM_IWAD to choose.
- The attract demos play instead of being rejected, and two 1997 bugs that
  filled the log with repeated warnings are fixed.
- The WebUI address now goes straight to the client that captures the mouse.

### 1.10.2
- The shareware game data ships with the container, so it plays as soon as it
  starts. Your own IWAD still takes precedence when you add one.
- The mouse is captured in the browser now, so turning no longer stops partway
  and the cursor cannot slide out of the window.
- The game claims the keyboard focus for itself instead of relying on where
  the pointer happens to be.

### 1.10.1
- Options - Setup, with three new pages: rebind keys, configure the mouse, and
  pick a WAD from the mounted folder without restarting the container.
- The mouse works. Pointer events never reached the engine before, and the
  filter for its own re-centring threw away most of the motion that did.
- The picture is no longer stretched: the browser client renders 320x200 at
  the scale you set instead of scaling it to the window.
- Fixed a crash on quit that hit roughly one exit in three.

### 1.10.0
- First release. The 1997 linuxdoom-1.10 sources repaired to build and run on
  64-bit Linux, with sound effects and music added, played in a browser over
  noVNC. Game data is not included; supply your own IWAD.
  </Changes>

  <Config Name="WebUI Port" Target="6080" Default="6080" Mode="tcp"
          Description="Port for the browser client, carrying both the picture and the sound. Open the WebUI to play."
          Type="Port" Display="always" Required="true" Mask="false">6080</Config>

  <Config Name="VNC Port" Target="5900" Default="5900" Mode="tcp"
          Description="Optional. Only needed if you want to connect with a native VNC client instead of the browser."
          Type="Port" Display="advanced" Required="false" Mask="false"></Config>

  <Config Name="WADs" Target="/wads" Default="/mnt/user/appdata/doom/wads" Mode="ro"
          Description="Optional. The shareware game data is already in the container. Put your own DOOM.WAD, DOOM2.WAD, TNT.WAD or PLUTONIA.WAD here to play the full game; it takes precedence over the shareware file. Mounted read only; the container never writes to it."
          Type="Path" Display="always" Required="false" Mask="false">/mnt/user/appdata/doom/wads</Config>

  <Config Name="Saves and config" Target="/doom/state" Default="/mnt/user/appdata/doom/state" Mode="rw"
          Description="Savegames, the .doomrc config file and the container's logs."
          Type="Path" Display="always" Required="true" Mask="false">/mnt/user/appdata/doom/state</Config>

  <Config Name="PUID" Target="PUID" Default="99" Mode=""
          Description="UID the game runs as. Must be able to write the saves folder. Unraid default: 99 (nobody)."
          Type="Variable" Display="always" Required="true" Mask="false">99</Config>

  <Config Name="PGID" Target="PGID" Default="100" Mode=""
          Description="GID the game runs as. Unraid default: 100 (users)."
          Type="Variable" Display="always" Required="true" Mask="false">100</Config>

  <Config Name="Scale" Target="DOOM_SCALE" Default="2"
          Description="Pixel scale, 1 to 4. The picture is 320x200 multiplied by this, so 2 gives 640x400. Larger values cost more bandwidth to the browser."
          Type="Variable" Display="always" Required="false" Mask="false">2</Config>

  <Config Name="VNC Password" Target="DOOM_VNC_PASSWORD" Default=""
          Description="Optional. If set, the browser and any VNC client must supply this password. Leave empty for no password, which is fine on a LAN you trust."
          Type="Variable" Display="always" Required="false" Mask="true"></Config>

  <Config Name="Audio rate" Target="DOOM_AUDIO_RATE" Default="22050"
          Description="Rate the sound reaches the browser at, in Hz. 22050 is the default and costs about 700 kbit/s. Use 11025 to halve that, or 44100 to double it."
          Type="Variable" Display="advanced" Required="false" Mask="false">22050</Config>

  <Config Name="PulseAudio server" Target="PULSE_SERVER" Default=""
          Description="Optional, and not what most people want. Leave empty and the sound goes to the browser. Set this to a reachable PulseAudio server, for example tcp:192.168.1.50:4713, and the sound goes there instead - to speakers attached to that machine, not to yours."
          Type="Variable" Display="advanced" Required="false" Mask="false"></Config>

  <Config Name="Soundfont" Target="DOOM_SOUNDFONT" Default=""
          Description="Optional. Path inside the container to a General MIDI soundfont for music. Leave empty to use the one built into the image."
          Type="Variable" Display="advanced" Required="false" Mask="false"></Config>
</Container>
